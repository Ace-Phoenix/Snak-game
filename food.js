class Food {
  constructor{
    this._type = type;//different types of food?
  }
  //getters
  get type(){return this._type;}

  //setters
  set type(type){this.type = type;}

  function makeFood(){
    
  }
}
